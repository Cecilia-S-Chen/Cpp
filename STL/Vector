/*array与vector区别
 * array:
 * (1)一经定义就不允许添加新元素，除非分配了新的内存空间
 * (2)只能包含整型字面值常量，枚举常量或者用常量 表达式初始化的整型const对象，非const变量以及需要到运行阶段才知道其值的const变量都不能用来定义数组的维度.
 * vector:
 * (1)空间灵活，可再加或减.提供push——back或者其他的操作在vector中添加新元素，
 *（2）vector可用另一个vector初始化，也可以将一个vector赋值给另一个vector
 * (3)vector是动态分配内存的，有专门管理内存的算法。它先给你开辟一段连续的空间，如果不够，它会根据设定的方式（倍数，指数，幂等……）开辟新的内存空间
 */
